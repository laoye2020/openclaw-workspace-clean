# 📝 Workflow Commands

## OpenClaw
```bash
openclaw status              # Check status
openclaw gateway restart     # Restart gateway
openclaw session status      # Session status
openclaw logs --follow       # View logs
```

## Trading Tools
```bash
# Polymarket
cd polymarket-tool && ./start.sh

# TA Telegram Bot
cd ta-telegram-bot && source .venv/bin/activate && PYTHONPATH=src python -m ta_bot.main
```

## Obsidian
```bash
cd ~/文档/laoye2025 && ./organize.sh   # Organize notes
```

## Git Backup
```bash
cd ~/文档/laoye2025 && git add -A && git commit -m "update" && git push
```

---
*Type: KNOW-002*
